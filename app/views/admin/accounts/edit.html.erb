<% @title = '社員管理 - 編集' %>
<h1><%= @title %></h1>

<div id="generic-form">
  <%= form_for [ :admin, @accounts ] do |f| %>
    <%= render 'form', f: f %>
    <div class="buttons">
      <%= f.submit '編集' %>
      <%= link_to '戻る', :admin_accounts %>
    </div>
  <% end %>
</div>
